# 重裝
# deactivate
# Remove-Item -Recurse -Force .\venv
# Test-Path .\venv
# python -m venv venv
# .\venv\Scripts\Activate.ps1
# python -c "import sys; print(sys.executable); print(sys.version)"
# python -m ensurepip --upgrade
# python -m pip -V
# python -m pip install -U pip setuptools wheel
# python -m pip install -r .\requirements.txt --no-cache-dir --force-reinstall
# python -c "import requests, urllib3, charset_normalizer; print('ok', requests.__version__, urllib3.__version__)"

# pip install --upgrade pip
# python -m pip install --upgrade -r requirements.txt


# 基礎 HTTP / JSON / 環境變數
requests
python-dotenv
urllib3

# 數據分析
tqdm
pandas
numpy
openpyxl

# 日期工具（pandas 已含，但保留）
python-dateutil

# 後端工具（你原本使用）
flask
flask-cors
gunicorn
requests

# 金融 API
shioaji
FinMind

# Messaging
python-telegram-bot
line-bot-sdk

# (可選) 用於回測/技術分析若你之後會需要
ta